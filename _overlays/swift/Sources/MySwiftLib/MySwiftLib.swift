// MySwiftLib — library entry
// `personalize.py` will rename the module + symbol names.

public enum MySwiftLib {
    public static let version = "0.1.13"

    public static func hello(_ name: String) -> String {
        "hello, \(name)!"
    }
}
